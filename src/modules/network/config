###############################################################################
# All our config settings must start with NETWORK_

[ -n "NETWORK_CONFIG_FILE" ] || NETWORK_CONFIG_FILE=/boot/${DIST_NAME,,}-wpa-supplicant.txt


