[ -n "$DOCKER_COMPOSE" ] || DOCKER_COMPOSE=yes
[ -n "$DOCKER_ADD_USER_TO_GROUP" ] || DOCKER_ADD_USER_TO_GROUP=no
[ -n "$DOCKER_COMPOSE_BOOT" ] || DOCKER_COMPOSE_BOOT=yes
[ -n "$DOCKER_COMPOSE_BOOT_PATH" ] || DOCKER_COMPOSE_BOOT_PATH=default

